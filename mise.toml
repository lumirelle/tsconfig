[env]
_.path = [ "./node_modules/.bin" ]

[tools]
bun = "latest"
aube = "latest"
pkl = "latest"
hk = "latest"

[hooks]
enter = "hk install"

[deps.auto-aube]
sources = [
  "package.json",
  "bun.lock"
]
outputs = [ "node_modules/" ]
# Because aube's output lockfile has bugs,
# we use `bun install --lockfile-only` to generate a lockfile
run = "bun install --lockfile-only && aube install"
auto = true

[tasks.pre-commit]
run = "hk run pre-commit"

[tasks.dev]
run = { task = "check" }

[tasks.build]
run = { task = "check" }

[tasks.start]
run = { task = "check" }

[tasks.check]
run = "hk check --all"

[tasks.fix]
run = "hk fix --all"

[tasks."test:watch"]
run = "vitest"

[tasks.test]
run = "vitest run"

[tasks.prerelease]
run = { tasks = [
  "check",
  "test"
] }

[tasks.release]
depends = "prerelease"
run = "aubx bumpp"
raw = true

[tasks.publish]
run = "aube publish --access public --tag latest"

[tasks."publish:commit"]
run = "aubx pkg-pr-new publish --bun"
