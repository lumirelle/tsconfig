{
  "extends": "@lumirelle/tsconfig/ts",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    // Include types you need, if you are using Node.js, please replace "bun-types" with "node".
    "types": ["bun-types"],
    // Allow importing JavaScript files
    "allowJs": true
  },
  // Only include your project's script files & config files who are expected to run without `isolatedDeclarations`.
  // Also root test cases which does not belong to any package.
  "include": ["scripts/**/*", "test/**/*", "*.config.js", "*.config.ts"]
}
