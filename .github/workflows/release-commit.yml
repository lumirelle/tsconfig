name: Release Commit

on: [push, pull_request]

permissions: {}

jobs:
  release-commit:
    uses: lumirelle/workflows/.github/workflows/release-commit.yml@v10
    with:
      build: ''
      cache-preset: 'mise-task,nub'
